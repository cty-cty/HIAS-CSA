# 培养规则修复与本地验收报告

日期：2026-09-09。范围：此前完整需求、后续批注，以及 2026-09-07 审计发现。此报告针对当前未提交的本地工作区，不代表线上已更新。

## 结论

审计发现的跨学期输入不一致、英语重复计分、教学班被合并、属性迁移、重复推荐、过度补核心、非学位候选被误排除、AI规则认定不完整及博士统一口径等问题已修复。保留原有页面布局、导航、存储和导出流程。

培养进度是“历史已修 + 各学期已选/计划”的去重累计，预选不是已修。跨专业补充学分与本专业2+2分别检查，不再设置先后门槛。未确认的正式规则保留待核验，不由推荐算法补造。

本地预览：http://127.0.0.1:4173/ 。没有提交、推送、合并或发布；请用户检查后再决定是否提交。

## 修改文件与职责

| 文件 | 修改内容 |
| --- | --- |
| `app/credit-model.ts` | 官方类别解析、四种认定状态、统一课程身份、历史/选课去重、英语family、原始属性读取、跨专业学分与本专业门数分离、适用对象检查 |
| `app/program-plans.ts` | 可选特殊规则、AI规则数据、普博/直博/硕博连读分轨、未知目标用null而非猜数 |
| `app/program-rules.ts`（新增） | 共享培养缺口、特殊规则评估、联合完成条件、学期硬检查、进度与待核验提醒 |
| `app/recommendation-engine.ts`（新增） | 有界动态方案生成、硬筛选、边际贡献、软排序、最终复核、完整方案指标及理由；不依赖React |
| `app/course-opportunities.ts`（新增） | 根据培养方案整理秋春机会；资料冲突返回unknown，机会只影响推荐策略 |
| `app/course-data.ts`（新增） | 计划/正式状态优先级、保留正式教学班、增量更新、已选ID及学位属性迁移 |
| `app/course-explorer.tsx` | 接入累计规则和推荐、分级提示、课程名标签、分类卡片、方案确认弹窗、导入恢复校验、未知信息展示、状态恢复/存储/分页稳定性 |
| `app/courses-spring.json` | 保留原记录，逐项核对四方向春季课程、适用对象与状态；没有删除重建或编造正式排课 |
| `scripts/validate-rules.mjs`、`scripts/validate-regressions.mjs`（新增） | 轻量规则、真实158门秋季数据四专业回归和导入兼容验证 |
| `scripts/ts-extension-loader.mjs`（新增） | 供现有Node工具链执行纯TypeScript规则测试，不引入大型测试依赖 |
| `scripts/normalize-spring-data.mjs`（新增） | 只读春季数据审计；虽保留normalize文件名，但不写回、不重新生成数据 |
| `scripts/fixtures/backup-v2.json`（新增） | 合成旧格式备份验收夹具，不是用户真实旧备份 |
| `scripts/fixtures/spring-sections-test.json`（新增） | 明确标记为合成测试的两班导入夹具，不进入内置课程，不是2027正式课表 |
| `package.json` | 增加轻量规则测试命令，无新增依赖 |
| `vite.offline.config.ts` | 使用当前构建器的单文件配置，保留离线打包方式 |
| `README.md`、`AUDIT-2026-09-07.md`、本报告 | 同步使用口径、计划数据来源、测试边界，并区分旧审计与当前修复状态 |

为通过全项目检查，另有小范围通用组件修正：`breadcrumb.tsx` 去掉伪链接角色；`button-group.tsx`、`field.tsx`、`input-group.tsx` 使用正确分组语义；`label.tsx` 明确标签关联；`pagination.tsx` 明确链接内容；`input-otp.tsx` 装饰分隔符不伪装交互元素；`item.tsx` 补齐列表语义；`spinner.tsx` 使用输出语义；`chart.tsx` 显式字符串转换；`carousel.tsx` 正确订阅与清理轮播状态；`hooks/use-mobile.ts` 订阅媒体查询。没有关闭检查规则、删除功能或更换整套设计。

`app/courses.json` 的158条秋季课程、`app/graduate-program-mapping.ts` 和 WakeUp CSV 格式未改写。

## 新规则架构与原问题修正

### 四层分离

1. 官方分类：可靠课程编号优先，缺失/不可解析才回退category。1/2/3有专业学位资格；4/5/6/7及公选不能作为专业学位课。
2. 培养认定：eligible、approval_required、verification、ineligible。课程池、学科映射和适用对象决定认定，不由当前课表所属专业覆盖学院方案。
3. 学期规则：当期有效学分、秋季必修、冲突、重复、体育数量及非法属性等。
4. 推荐策略：在合法用途基础上比较缺口、机会、课业和时间；“不推荐”不等于“不能选”。

规则优先级为学校明确禁止/强制、学院具体方案、学校通用最低要求；当前课表确认实际开课和排课，跨专业认定须查正式材料，最后才是网站偏好。所有判断为确定性代码，无AI硬规则判断。

### 学分与课程认定

- HIAS讲堂进入公共选修，不进入专业非学位；科学前沿进入专业非学位，不进入公共选修。两者都不计入学期最低10学分，历史记录采用同一归属。讲堂保留累计学时/学分换算说明。
- 跨专业官方核心/专业课设为degree后立即纳入专业学位规划合计；用同一张卡说明“方案认可范围内 + 非本专业专业类课程”。不要求先修满2+2，但跨专业课程不替代自身方案核心/专业门数，也不冒充已经完成或学校已认定。
- 默认推荐只用本专业明确池补专业学位确定缺口。主动开启跨专业扩展后可展示不确定候选，理由、预览和应用使用同一designation。
- 课程名以独立标签显示；提醒用户查学校官网、学院方案、教务系统确认学位属性。网页不提供导师审批；所有课程安排都建议与自己的导师确认合理性。
- 学硕按一级及允许的二级学科判断；映射不足为verification，不自动禁止。当前课表标注本人专业但方案池未列出的课程为资料待核验。
- 联合专业学位完成必须同时满足学分、本人核心门数、本人专业门数和specialRules。仅eligible且实际设为degree的合适类型计入确定门数。
- AI特殊规则是数据驱动的通用评估：至少一门《高级人工智能》《自然语言处理》作为有效核心学位课；仅名称相同、但认定待核验或官方类型不符不能通过。
- 普通公选2 + 创新创业1 = 公选体系3。两个分项分卡展示，创新创业不再额外重复累计，也去掉“其中：”。

### 博士与来源核对

重新核对本地《国科大杭州高等研究院课程学习及选课须知-2026-2027学年.pdf》第10、12、14–16、22–23、30、57页，以及《物光学院2026-2027课程按专业合并整理.xlsx》四个方向的春季条目和汇总行。PDF第10页另做页面渲染核对。

- 普博：公共必修5、专业学位至少4；至少1门本学科硕博通用或博士专属核心/专业学位课。学院完整要求仍待核验，不套2+2，不展示猜测的38总学分。
- 直博、硕博连读：公共必修11、专业学位至少16，结合对应材料检查核心2+专业2；完整总学分未知，仍显示待核验。
- 旧物理电子学博士ID保留并明确标注普通招考博士；培养设置中可改选直博/硕博连读。不能仅凭旧记录猜测实际入学类型。
- 历史记录缺少课程层次时，不能假定满足普博指定课程层次要求。硕士不计入博士公共必修课程作为毕业必需学分。
- 最终页面回归补修：从硕士切换至普博时，硕士英语、自然辩证法、中特理论课程不会再误填普博公共必修5学分；原选择保留，其其他用途提示核验。原硕士英语免修资格也保留，但不会在普博方案中自动加3分或抵消已选课程。

### 累计进度与本学期检查

体检与推荐都读取同一`calculateProgramGaps`。历史与所有学期选课合并去重；当前学期另传课程用于排课、冲突、10学分、必修和导出。

红色只用于本学期必须处理；累计培养缺口是黄/绿进度；资料和认定不确定是黄色提醒。核心1/2但春季有计划课时显示可在春季继续完成，不当秋季错误。

英语免修3分只累计一次，不删除已选英语，不增加课程门数。春季硕士英语与秋季英语A为同一family。若当期实际学分不足10、但加免修可达10，因材料未明确口径，仅显示黄色待确认，不直接报错；即使加免修也不足10则仍提示学期不足。

### 推荐、机会与教学班

保留当前已选为锁定条件，最多3个不同目标、每组最多10次添加。每一步重算边际贡献，剔除历史/其他学期已选的同课程、冲突班次、不适用层次和明确不合法的用途；合法公选/伦理等非学位用途不会误排除。

缺口补满后不再重复奖励；当期硬要求处理完即停止，不为了毕业总量无限加核心。缺少专业非学位时，可把额外合适课程建议为非学位，而不是一律degree。春季剩余机会降低秋季补齐紧迫度；“秋季机会”只是策略，不是学校强制。

按教学班选择：01冲突但02不冲突时可选02；不重复推荐同一培养课程。最终重新计算实际冲突；计划课无时间不参与冲突，但明确标记排课待核验，不能宣称保证无冲突。

预览展示分类学分、门数、有效学分、冲突、周末和考核等指标、理由及待核验课程。应用前列出新增与保留，并明确移除0、换班0，用户确认后才写入；本轮不自动删除或替换锁定课程。结果相同时合并，不硬凑三套，也不称绝对最优。

## 春季数据审计与更新

原22条保留：物理电子学5、光电6、人工智能3、材料3，共17条学院春季/秋春课程；另有5条学校公共计划课。逐项核对名称、学分、层次、专业、类别和已有教师字段；源材料没有的信息保持未知，物理电子学仅标“核心课”的不猜具体核心子类。

FPGA为秋春同一培养课程。春季公共选修实际名单未知，不生成具体课程。2026一年级秋季的中国特色社会主义理论与实践、自然辩证法不被建议拖至春季。

planned状态只能做规划：无假星期节次、无假容量，内部SP编码不显示成官方编号。正式JSON优先，按官方编码、canonical身份、标准化名称+专业匹配；不同正式班次分别保留，匹配计划记录升级，旧选择与属性迁移，未匹配的旧数据保留。多班次匹配需先确认拟选班次，导入后仍可换班。

## 兼容与验证结果

保留存储键、按学期选择、designation、历史、英语状态及旧备份入口；没有清空常用127.0.0.1页面。新方案字段可选；旧合法方案不含specialRules/studentTrack仍可用。显式unset/non-degree不会被默认建议覆盖。旧数据通过延后恢复且storageReady后再保存，避免初次渲染空状态覆盖记录。

| 检查 | 结果/边界 |
| --- | --- |
| `npm run test:rules` | 通过：基础规则及身份/历史/英语/跨学期/导入/推荐回归；四方向真实158门候选总耗时约2–4秒（最后一轮与构建并行），非手机性能基准 |
| `npm run lint` | 通过，未禁用规则 |
| `npx tsc --noEmit --incremental false` | 通过 |
| `npm run build` | 通过产物校验；Node24/Windows退出阶段出现已有句柄断言，由原构建脚本验证产物后处理，非完全无警告；不等于发布 |
| `npm run build:offline` | 通过，约0.84MB单文件，158条秋季、22条春季；无外部脚本/样式依赖 |
| 春季只读审计 | 22条通过；17条指定学院课程均在 |
| 四专业页面流程 | 光电、AI、材料、物理学硕均完成培养设置、推荐、预览应用与进度核对 |
| 跨学期累计 | 秋季核心门数在春季保留；当前课表/有效学分独立；推荐不重新补已选核心 |
| 跨专业补充 | 页面确认补充学分即时加入总计，核心/专业门数不冒增；课程名标签与分段说明可读 |
| 推荐确认 | 预览与应用学分/属性一致；取消不修改；锁定课程保留；不同结果去重 |
| 冲突/换班/撤销 | 制造同周冲突显示红色；换班消除；立即撤销恢复；不增课程门数 |
| 英语免修 | 获得/取消不删除已选英语，培养累计不重复；最低学分不确定口径为黄色 |
| 博士分轨页面 | 普博公共必修目标5、专业目标4，核心/专业门数待确认；直博公共目标11、专业目标16，独立2+2；硕士公共课不自动补普博公共要求 |
| `git diff --check` | 通过，仅Windows换行提示 |
| 备份 | 当前v4导出/预览/恢复通过；合成v2格式夹具恢复通过，不声称覆盖所有用户历史备份 |
| 春季正式更新UI | 合成两班JSON导入前出现匹配确认；22条变23条（占位1替为正式2）；仍只选1门，degree保留；两个教学班均可检索，无多余计划占位 |
| WakeUp CSV | 实际下载并核对原7列格式；5门测试课程生成6条时间安排；未在WakeUp手机App内二次导入 |
| 移动端 | 390×844检查提醒标签与分段可读、正文无横向溢出；不是全部真机覆盖 |

独立localhost页的合成正式课表已通过恢复测试备份移除，测试夹具保留在仓库便于复验。没有删除真实用户文件；测试下载文件未擅自清理。

最终只读打开常用127.0.0.1预览，原秋季8门与春季3门均仍存在；培养页显示专业学位规划18.5/12（方案认可范围内12.5 + 非本专业6），核心2门、专业3门，普通公选0/2、创新创业0/1。未改动这些选择或属性。

## 待学院/教务确认与明确保留的边界

1. 普博学院完整规则及各博士培养类型总学分，现有材料不能补出统一总量。
2. 英语免修是否计入本学期最低10学分，需教务确认。
3. 《自然语言处理》学校材料春季与学院表秋季存在冲突：实际秋季课表可用于当前排课，但机会状态为unknown，不给“只有秋季”奖励。
4. 2027春季正式开设、编码、班次、时间、教室、容量及未载明师资/考核，均待正式课表。当前计划不是保证开课。
5. 非本专业课程最终学位认定、学科映射不足及历史层次缺失，需要正式资料；工具仅提供提醒，不提供审批。
6. 缺失原格式必需字段的自定义方案仍会被安全拒绝并提示，不会猜要求或崩溃；支持的是旧合法格式和可选新增字段。没有实现任意残缺JSON自动补全。
7. 英语MOOC/英语A三路径等P1扩展未重做；没有允许自动删除锁定课程的优化模式；高级研究方向偏好未加入长问卷。
8. 四专业分别完成主流程，共用备份、换班、导出等功能做了专项回归，未声称对每个专业逐一重复所有排列组合。没有做全部真实手机、WakeUp应用内或线上发布验收。

上述不确定项不应以“通过自动测试”代替学校确认。代码、本地预览及离线版等待用户检查，不自动提交。
