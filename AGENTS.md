# HIAS-CSA 项目维护约定

## 修改记录

每次修改本项目的规则、课程数据、培养方案、推荐逻辑、界面、兼容性或测试，都必须同步更新：

`docs/HIAS-CSA-完整修改清单与选课逻辑验收说明-2026-09-09.md`

新增记录至少包含修改日期、需求背景、涉及文件、实现逻辑、用户可见变化、兼容性、测试结果、本地预览状态和待确认事项。不要只修改代码而跳过主文档记录。

从 2026-09-14 起，每次更改（包括文档更改）按以下统一格式追加记录：

- 标题：日期 + 简短主题；正文先说明需求背景。
- 修改内容：已有行为、规则或数据的修正，写清修改前 → 修改后及原因。
- 优化内容：已有体验、性能、可读性或维护性的改善及具体收益。
- 新增内容：新增功能、文件、测试或说明及用途。
- 删除内容：移除的功能、数据或文件、原因、影响及恢复方式；没有则写“无”。
- 影响范围：涉及文件、用户可见变化、存储/备份兼容性与迁移需求。
- 验证结果：实际执行的检查及结果，明确未测、失败、已知问题和待确认事项；文档改动不冒用以前的构建结果。
- 发布状态：区分本地完成、已提交、已推送、部署成功、线上已验证；有提交或部署证据时记录编号/链接，本地预览附地址及实际验证范围。

四个内容类别均保留，没有涉及的写“无”；同一变更在最适合的类别中详述，其他类别无需重复。历史条目保留原始记录，补充结果时标注复核日期，不能把计划验证写成已通过。

## 受控修改

- 保留既有功能、localStorage、备份恢复、离线版和 WakeUp CSV 格式。
- 不删除或重新生成既有课程数据，除非用户明确要求并完成审计。
- 新增学院或培养方向时，优先补充学院目录和培养方案数据，让筛选分组自动生成，不在页面组件中硬编码学院列表。
- 规则使用确定性代码执行；不把学校培养硬规则交给 AI 自行判断。
- 发布前先完成本地预览和测试，未得到明确授权不得自动推送或替换线上版本。
