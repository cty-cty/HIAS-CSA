# 培养规则与选课逻辑审计（2026-09-07）

> 状态更新（2026-09-09）：本文保留修复前的审计证据，行号和“未修复”结论对应当时版本；后续修复、材料核对及验收结果请见 [修复与验收报告](FIXES-2026-09-09.md)。不要将下文旧状态当作当前测试结果。

## 结论与范围

依据本次对话最初的 23A–23G、0–46 项要求，以及后续关于跨专业学分、跨学期累计、公选 2+1、课程提醒和排版的修正，审阅当前本地实现。整体方向正确，但统计、导入、推荐和展示尚未形成一致的完整流程，不能认定整项任务已验收。

本次只进行代码审阅、只读复现实验、已有测试及构建检查；没有修复业务代码，没有提交、推送或发布。本文为本地审计交付物。

本轮未重新逐页核验学校 PDF 和学院 XLSX，也没有执行四个专业全部浏览器操作场景。涉及官方材料完整性、准确学分和博士培养要求的结论因此保留待核实状态。编译和自动测试通过不能替代这些验收。

## 应采用的最终产品逻辑

- 培养规划累计：历史已修 + 各学期已选/计划；同课程去重，保留来源、学期、认定状态，预选不等于已修。
- 当前学期：课表、时间冲突、换班、有效学分、体育数量、必修检查和 WakeUp 导出按当前学期处理。
- 非本专业专业类课程：设置为学位课后可以加入规划学分合计，无须先完成本专业 2+2；本专业核心与专业门数独立验证。按当前材料可确认与需进一步查证的份额应分开解释。
- 公共选修显示：普通公共选修 2 + 创新创业 1，合计 3，不显示成额外 4 学分。
- 提醒：列出相关课程，建议查阅学校官网、学院培养方案和教务系统确认学位属性；所有课程安排均建议与导师确认合理性。网页不表示真实审批状态。
- 推荐：共享累计培养进度，以当前学期生成班次；每次加入候选重新计算缺口，最终再检查整套方案；明确展示不能完成的部分。
- 春季计划数据：可以参与规划，不能冒充正式排课。正式更新既要升级计划记录，也要保留各个教学班和用户选课属性。

## 需要优先处理的问题

### 1. 培养体检与推荐读取不同学期范围（高优先级）

`app/course-explorer.tsx:2037` 将累计课程交给体检，但 `:2224` 向推荐传递的仍是 `countedSelectedCourses`（当前学期）。推荐没有读取其他学期选课。

复现实验：秋季选了两门核心，春季未选；累计体检核心=2，按推荐当前接口计算核心=0。可能重复补核心，或推荐已在其他学期计划的课程。推荐对历史课程也没有做候选去重；实测历史已修《集成与微纳光子学》仍被三个方案推荐。

### 2. 导入把同课程不同班次合并成一条（高优先级）

`app/course-explorer.tsx:908` 的 `mergeCourseRows` 使用课程身份合并所有记录，未区分计划占位和正式教学班。只读执行现有函数：输入正式 01/02 两个班，输出只剩一个班。

此外，正式记录仅以 `scheduleStatus === 'confirmed'` 判断优先级。实测缺少该可选字段的旧格式正式课程与计划记录同批导入时，仍保留 planned。更新流程只对新输入内部合并，没有可靠的旧计划部分升级流程，也没有迁移编码变更后的 designation。原选课中未匹配的 ID 会在更新时被替换掉。

### 3. 春季硕士英语免修重复累计（高优先级）

`app/credit-model.ts:443` 仅匹配旧英语代码或以“英语”开头的名称，未识别春季占位课程《硕士学位英语》。实测仅选该课程并标记免修后，`selectionCredits` 为 6，期望为 3。

同时，培养摘要、学期有效学分对免修的口径不同：前者加 3，后者不加。是否将免修计入学期最低要求，应依据正式材料明确；至少不能用不同口径而无说明。用户截图里的总量 10.5、有效 7.5 正是这种差异。

### 4. 推荐会过度补核心，且三个方案可以相同（高优先级）

`app/recommendation-engine.ts:378` 只相对于初始选课计算贡献，`:324` 搜索时直接叠加固定得分，不随组合状态更新缺口；有效学分分数也不会在最低要求满足后停止增加。

使用真实光电核心课程，仅去掉排课以隔离评分因素：已有一门核心，三个方案都再加入四门，输出核心 5/2。三套方案课程集合相同，仅次序不同。18 个候选、10 个保留状态和最多 4 门的上限限制了复杂度，但不保证方案合理或完整。

### 5. “不能作为学位课”被用作“不能推荐任何用途”（高优先级）

`app/recommendation-engine.ts:399` 直接排除 recognition 为 ineligible 的课程。而公共选修、工程伦理、HIAS、科学前沿等该状态仅表示不能作为专业学位课，仍有合法非学位用途。

实测候选只有《工程伦理》时，所有方案都没有新增课程；创新创业、公选及专业非学位需求同样无法通过这条筛选。当前推荐不足以形成含公共必修、公选、创新创业、专业非学位的完整方案。

### 6. 旧 2+2 前置条件仍留在推荐中（高优先级）

`app/recommendation-engine.ts:365`、`:402` 仍以 `ownStructureSatisfied` 决定默认是否开放跨专业候选。后续对话已经明确 2+2 不是计入学分的先决条件，但代码和测试仍固化旧门槛。

本专业优先可以作为推荐策略；是否展示需查证课程可以独立设置，不能再解释成学校规定的先后顺序。

### 7. 推荐理由、预览学分和应用属性不一致（高优先级）

跨专业候选的理由声称能补学位学分，但贡献模拟默认将其设为非学位；最终方案及 UI 应用也只给 eligible 核心/专业课程写入 degree。

实测满足 2+2 后推荐《广义相对论》：理由写增加 3 学分跨专业补充，预览 `approvalRequiredDegreeCredits` 却为 0；应用也不主动设置为学位课。应让候选携带明确的建议 designation，预览与应用使用同一份结果。

### 8. 课程去重及跨学期属性合并不统一（高优先级）

UI 用 `coursesShareIdentity` 去重；底层学分使用 canonical ID；门数函数未对所选课程去重；英语另有 family。实测同一核心课两班传给 `getPlanCourseCounts`，计为两门。

`app/course-explorer.tsx:1127` 把所有学期属性直接对象展开合并，未限定对应的已选课程。后一学期残留的属性可以覆盖前一学期仍在选的同课程属性，结果取决于存储顺序。

### 9. 特殊规则与培养缺口模型不完整（中高优先级）

`app/program-rules.ts:97` 特殊规则仅检查课程名和 degree 标记，不要求 eligible，也未校验核心官方类型。实测将 AI 方案映射改成资料不足状态，《自然语言处理》recognition 为 verification，但特殊规则仍通过。

`publicRequired` 当前只是方案要求的非学位公共课名称列表，不是真实缺口；没有专业学位学分缺口、专业非学位缺口等完整输出，也没有统一的“学分 + 核心门数 + 专业门数 + 特殊规则”联合完成结果。页面分别显示一些进度，不等于联合验收已实现。

### 10. 春季机会目前主要停留在提示（中优先级）

内置 158 条秋季数据无 `semesterNote`，所以机会分类均不能识别为 fall_only/both。推荐虽接收 futureCourses，但评分未使用剩余春季机会降低秋季紧迫度。春季机会计数也没有剔除已选、已修或其他学期已经规划的同课程。

22 条春季记录均为 planned，排课为空，容量与人数为未知；17 门要求点名的课程均存在。这验证的是当前数据形态，不是所有官方春季课程与教师、学分等字段的完整逐项核验。

### 11. 博士和适用对象处理只做了一部分（中高优先级）

普通博士已不再套 2+2，但 `app/program-plans.ts:211` 仍显示 38 总学分、11 公共必修学分，未随普通博士材料不完整状态同步设为待核验。界面没有普通博士、直博、硕博连读的完整选择与对应规则。

课程适用对象在浏览时主要体现为标签；学分认定没有统一校验 applicablePrograms/applicableStudentTracks，用户手动添加不适用课程仍可能被统计。推荐也未使用普通 `level` 字段进行全面培养层次筛选。

### 12. 文案与来源说明仍有误导风险（中优先级）

- “已确认 12.5”包含预选和春季计划课程，实际仅能表示按现有材料匹配，不能表示已修或导师已审批。建议使用“本方案认可范围内 … + 非本专业专业类课程 …”，另行标明预计累计。
- 所有课程需与导师讨论的建议只在有待核验课程时显示，正常本专业方案看不到该通用提示。
- 历史跨专业学分进入了学分合计，但课程名称提醒仅扫描已选课程，实测有 3 历史跨专业学分时提醒名称仍为空。
- verification 不一定表示跨专业；课表所属专业与培养池冲突尚未有独立判定分支，需避免误标。
- 推荐的 `conflicts` 固定返回 0；已有锁定课程本身冲突时没有最终重算。时间未知、需查证认定等不同原因也混用了同一句“时间待定”说明。
- 推荐 UI 未展示完整的专业非学位、公选、创新创业和考核结构；已计算的理由未完整展示。夏季仍有固定 `/10` 文案。
- README 对跨学期累计等后续修改尚未全部同步。

## 对原要求的覆盖情况

| 要求范围 | 当前状态 |
| --- | --- |
| 0 审计；1、31、34 分层与规则优先级 | 已抽出纯规则文件，但上述重复判断、输入范围和推荐误用说明分层尚未完成 |
| 2、3、5、6、28–30 认定、分类、跨专业、映射 | 四状态和基本编号解析存在；学硕映射有限；冲突来源与适用对象校验缺失；推荐误用认定状态 |
| 4 HIAS/科学前沿 | 当前培养归属及最低学分排除基本正确；历史 HIAS 的原始 category 字段仍可能为专业选修，类别明细需一致 |
| 7、8 2+2 联合要求与 AI | 基本门数和 specialRules 存在，联合完成结果、特殊规则认定校验、去重不足 |
| 9 博士 | 普博门数已待核验，完整分轨及总量口径尚未落实 |
| 10 公选 | 当前已按对话显示普通 2 + 创新创业 1；总量只累计一次，测试覆盖了一门创新创业的分项 |
| 11–13 学期要求、秋季必修、英语 | 基本检查存在；英语跨学期 family 有可复现错误；年级/培养阶段不是完整 profile；免修最低学分口径待明确 |
| 14、15 课程身份与冲突 | 周次交集实现存在；多处 identity、导入教学班、累计属性、计划课程状态隔离仍需统一 |
| 16、17、41 体检分类与共享缺口 | 红黄分类已接入；缺口字段不完整，推荐和体检输入不同 |
| 18–21、24–26、32、33 完整方案、贡献、算法、解释 | 有界搜索、锁定和确认存在；完整性、动态边际贡献、非学位候选、最终校验、方案差异与解释不足 |
| 22、23、23F 跨学期机会 | 已有提示，评分策略和剩余机会计算未完成 |
| 27、29 推荐偏好及扩展 | 有三种固定目标，无完整偏好与主动扩展跨专业选项 |
| 35、36 旧数据/自定义方案兼容 | 原存储键和旧备份入口保留，新方案字段多为 optional；课程更新属性迁移、重复记录、自定义方案边界缺测试 |
| 37、38、39、40 功能、设计、入口、锁定 | 保留现有设计和操作主体，有确认应用；正式班次被导入合并会破坏换班体验 |
| 23A、23B 春季课程审计/公共课 | 17 门点名课程存在，公共计划课存在；本轮未重新逐条核对完整官方材料 |
| 23C、23D 计划状态与未知数据 | 内置状态、空排课、未知容量和编码提示已实现；正式/计划状态在所有导入和逻辑路径中未统一 |
| 23E 正式更新 | 存在严重班次合并、旧格式优先级和属性迁移问题 |
| 23G、42–44 自动与人工验收 | 已有轻量脚本，但覆盖不足；四专业完整浏览器流程尚不能宣布通过 |
| 45、46 禁止项与交付 | 本轮未推送、合并、清理用户数据；目前只能交付审计结论，不能宣布全部实现完成 |

## 验证结果与测试盲区

- `npm run test:rules`：通过。
- `npx tsc --noEmit --incremental false`：通过。
- `npm run build`：通过，产物验证成功；Windows Node 退出阶段仍有现有断言警告，由构建脚本检测产物后处理。
- `npm run build:offline`：通过；校验输出 158 门秋季课程，单文件约 0.82 MB，无外部脚本和样式。
- `git diff --check`：通过，仅换行格式提示。
- `npm run lint`：失败；包含 UI/配置已有错误，另外 `app/course-explorer.tsx:2047` 的多余依赖是本轮跨学期修改引入的，不能概括为全部都是旧问题。

测试脚本把两门不同核心课赋予同一 canonical 编码，却断言核心门数为 2；这会掩盖 identity 问题。冲突测试断言 `plan.conflicts === 0`，但该值在实现中写死，不能证明无冲突。测试还在验证旧的“2+2 后开放跨专业”逻辑。后续测试应优先覆盖本次实际复现，而不是仅延续这些成功断言。

建议修正顺序：统一身份与跨学期输入 → 导入班次和属性迁移 → 英语去重 → 推荐目标与认定分离、动态评分和最终校验 → 完整缺口与特殊规则 → 博士/数据源核验 → 文案与四专业人工验收。
